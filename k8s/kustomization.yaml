apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: gokube
  environment: production
  version: v1.0.0

namespace: gokube

images:
- name: gokube
  newTag: latest 