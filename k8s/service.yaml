apiVersion: v1
kind: Service
metadata:
  name: gokube
  labels:
    app: gokube
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: gokube 